import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.anderson.app',
  appName: 'dev.anderson.app',
  webDir: '../../dist/apps/mobile/',
  bundledWebRuntime: false
};

export default config;
